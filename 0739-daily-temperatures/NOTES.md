The brute force approach will take O(n**2) because we need to traverse the array twice.
​
We will use monotonic decreasing stack to solve this problem.
​
Time complexity = O(n)
Space complexity = O(n)